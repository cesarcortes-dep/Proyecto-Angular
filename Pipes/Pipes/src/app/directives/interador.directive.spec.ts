import { InteradorDirective } from './interador.directive';

describe('InteradorDirective', () => {
  it('should create an instance', () => {
    const directive = new InteradorDirective();
    expect(directive).toBeTruthy();
  });
});
